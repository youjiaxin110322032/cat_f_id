services:
  - type: web
    name: cat-face-id-api
    env: python
    plan: free
    pythonVersion: 3.12.5
    buildCommand: >
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: uvicorn api.index:app --host 0.0.0.0 --port $PORT

